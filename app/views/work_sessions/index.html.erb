<h2>Séances</h2>
<div class="card">
  <div class="table-responsive">
    <table class="table">
      <thead>
        <tr>
          <th>Date</th>
          <th>Créneau</th>
          <th>Classe</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @work_sessions.each do |work_session|%>
          <tr>
            <td><%= work_session.date %></td>
            <td><%= work_session.daily_schedule %></td>
            <td></td>
            <td><%= link_to "Voir", work_session_path(work_session.id), class: "btn btn-primary" %> <%= link_to "Editer", edit_work_session_path(work_session.id), class: "btn btn-secondary" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>